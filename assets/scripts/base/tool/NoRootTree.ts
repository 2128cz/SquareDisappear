const { ccclass, property } = cc._decorator;
import { RigorousRingBuffer as ksimit } from '../class/RigorousLibrary';
@ccclass
export default class NewClass extends cc.Component {

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {}

    start() {

    }

    // update (dt) {}

    // TAG USER FUNCTION:                                                                                    
    
    ksimit
}
